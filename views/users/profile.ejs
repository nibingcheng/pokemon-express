<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=>, initial-scale=1.0">
    <title>Document</title>
    <link rel='stylesheet' href='/css/app.css'>
</head>
<body>
    <h1>Welcome <%=userInfo.name%></h1>
    <h3>I belong to this team: <%=userInfo.Team.name%></h3>
    <form action="/users/profile/<%=userInfo.id%>?_method=PUT" method="POST">
        Name: <input type="text" name='name' value="<%=userInfo.name%>"><br/>
        Username: <input type="text" name='username' value="<%=userInfo.username%>"><br/>
        Password: <input type="text" name='password' value="<%=userInfo.password%>"><br/>
        <input type='submit' value='Edit'>
    </form>

    <select name="team">
        <% for ( let i = 0; i < teams.length; i++ ) { let selected = ( i == 0
        ) ? "selected" : ""; %>
        <option value="<%=teams[i].id%>" selected>
          <%=teams[i].name%>
        </option>
        <% } %>
    </select>

    <form action='/users/<%=userInfo.id%>?_method=DELETE' method='POST'>
        <input type="submit" value="Delete User">

    </form>
    
    <a href="/users">Back to login page</a><br/>
    <a href="/pokemon">View All Pokemon</a>
</body>
</html>