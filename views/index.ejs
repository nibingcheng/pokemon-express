<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title></title>
        <link rel='stylesheet' href='/css/app.css'>
    </head>
    <body>
        <h1>Pokemon show page</h1>
        <ul class='pokemon'>
            <% for (let i=0; i<pokemon.length; i++) { %>
                <!-- <li><%= pokemon[i].name %> <img src="<%= pokemon[i].img %>" alt="pic"></li> -->
                <!-- <li><a href="<%= pokemon[i].img %>"><%= pokemon[i].name %></a></li> -->
                <li>
                    <a href="/pokemon/<%= i; %>"><%= pokemon[i].name %>.....</a>
                    <button><a href="<%= pokemon[i].img %>">img</a></button>
                </li>
            <% } %>
        </ul>
    </body>
</html>    